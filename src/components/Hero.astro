<section class="hero">
  <div class="index"><span class="num">001</span> - Software Engineer / San Jose, CA</div>
  <h1>Full-stack. Shipped <span class="accent">solo</span>.</h1>
  <div class="lede-box">
    <p class="lede">
      <span class="prompt">&gt;</span> Kevin Galvan. Full-time software engineer working across the stack.
      I also independently built and still own Conosi, an app I took solo
      from an empty repo to the App Store.
    </p>
  </div>
  <div class="cta-row">
    <a class="btn" href="/#projects">View Projects</a>
    <a class="btn" href="/resume.pdf" download>Download Résumé</a>
  </div>
</section>

<style>
  .hero {
    padding: 90px 48px 60px 48px;
  }

  .index {
    font-family: var(--font-display);
    font-weight: 500;
    font-size: 12px;
    letter-spacing: 0.04em;
    color: var(--color-accent);
    margin-bottom: 28px;
    padding-bottom: 14px;
    border-bottom: 1px solid var(--color-border);
  }

  .index .num {
    color: var(--color-accent-2);
  }

  h1 {
    font-family: var(--font-display);
    font-size: 46px;
    font-weight: 700;
    line-height: 1.15;
    letter-spacing: -0.01em;
    text-transform: uppercase;
    margin: 0 0 24px 0;
    max-width: 720px;
  }

  .accent {
    color: var(--color-accent);
  }

  .lede-box {
    border: 1px solid var(--color-border);
    padding: 20px 24px;
    max-width: 560px;
    margin-bottom: 34px;
  }

  .lede {
    font-family: var(--font-mono);
    font-size: 14px;
    color: var(--color-text-dim);
    line-height: 1.65;
    margin: 0;
  }

  .prompt {
    color: var(--color-accent-2);
    margin-right: 6px;
  }

  .cta-row {
    display: flex;
    gap: 16px;
  }

  .btn {
    font-family: var(--font-mono);
    font-size: 12px;
    letter-spacing: 0.03em;
    text-transform: uppercase;
    padding: 12px 20px;
    border: 1px solid var(--color-border-strong);
    text-decoration: none;
    font-weight: 600;
    transition:
      background-color 0.15s,
      color 0.15s,
      border-color 0.15s;
  }

  .btn::before {
    content: '[ ';
    opacity: 0.5;
  }

  .btn::after {
    content: ' ]';
    opacity: 0.5;
  }

  .btn:hover {
    background: var(--color-accent);
    color: var(--color-bg);
    border-color: var(--color-accent);
  }

  @media (max-width: 768px) {
    .hero {
      padding: 60px 24px 40px 24px;
    }

    h1 {
      font-size: 34px;
    }
  }

  @media (max-width: 480px) {
    .hero {
      padding: 48px 16px 32px 16px;
    }

    h1 {
      font-size: 26px;
    }

    .lede {
      font-size: 13px;
    }

    .cta-row {
      flex-direction: column;
      align-items: flex-start;
      gap: 12px;
    }
  }
</style>
